import { Vector3 } from "three";

/** Represents the size of a block in world units. */
export const BLOCK_SIZE = 1;

export const BLOCK_OFFSET = new Vector3(-BLOCK_SIZE / 2, -BLOCK_SIZE / 2, -BLOCK_SIZE / 2);